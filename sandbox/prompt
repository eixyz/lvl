Great, now we move to another task, and here comes the new functionality, like from the picked first arrivals, we want them corrected to the real offset. We have the geometry that we load which has values like 0 to x, but the shot point was not exactly at the first position or so, so for that we want to calculate the real offset, like for chanel c we will have C_k = sqrt((x_k-SP)**2+PO**2) where k SP is the shotpoint position and PO is the perpendicular offset where the source is located relative to the profile line. So we need PO for each SP and we compute new x or the real offset and here we use the values from geometry as we are already doing for the plotted offset on the top of the figure when we type c. And with those corrected offset, we correct also the picked traveltimes (new columns in the excel file), so that we do not modify the correct picked ones and then we plot those against channel or offset (one visualization QC). From those new picks, one interpolates along the real (original) x values for that we know the t values from x=0 to x=k and this are plotted and from this new plot we pick the x values where the layer changes for example in a 2L case we chose where x changes in the first and last shot and if we have centre as it is in the current data, the centre shot has usually two sides so we pick both and then one computes the velocities for each layer, So our starting point is that we implement for 3 layers, and if there is no third layer or in one shot point only one layer is visible a value of 0 is kept and skipped on the final computations. So, our program/plot should allow the user to enter the perpendicular offsets and for layer estimations should also allow to pick x0-x1 which is required for v0, x2-x3 (x2 usually = x1) for v1 and x4-x5 (x4->x3) for v2 and for each, the intercept time is also computed and the layer depth too and one can also display them as the headers in the FB pick plot and the final are saved in the excel.